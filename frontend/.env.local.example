# Base URL of the FastAPI backend, including the versioned API prefix.
# Keep the same hostname as the frontend (localhost, not 127.0.0.1): the
# session is an HttpOnly cookie set by the backend, and proxy.ts only sees it
# when both share a host. See "Docker" in README.md.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
